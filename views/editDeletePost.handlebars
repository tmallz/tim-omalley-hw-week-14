    <div class=" mx-10 my-10">
    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
        <div class="flex flex-wrap -mx-3 mb-6">
        <div class="w-full px-3 mb-6 md:mb-0 bg-gray-800 text-white">
           <a href="">{{post.title}}</a>
            {{!-- <span id="postDate" class = "origin-top-right absolute right-20 text-white">{{user.username}} {{format_date createdAt}}</span> --}}
        </div>
    </div>
        <div class="block">
            <span class="text-gray-700">{{post.body}}</span>
        </div>
      <div class="my-3 flex justify-center">

        <button class="bg-gray-800 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded mx-2" id="deletePostBtn">
          Delete Post
        </button>
      </div>
    </div>
    </div>

<div class=" mx-10 my-10">
  <form class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
      <div class="flex flex-wrap -mx-3 mb-6">
    <div class="w-full px-3 mb-6 md:mb-0">
      <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-first-name">
        Title 
      </label>
      <input class="appearance-none block w-full bg-white text-gray-700 border border-black rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white" id="updatedTitle" type="text" placeholder="New Title">
    </div>
  </div>
    <label class="block">
        <span class="text-gray-700">Note</span>
    <textarea class="form-textarea mt-1 block w-full" rows="3" placeholder="Updated note goes here." id = "updatedBody"></textarea>
    </label>
    <div class="my-3 flex justify-center">
        <button class="bg-gray-800 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded mx-2" id="editPostBtn">
          Edit Post
        </button>
    </div>
  </form>
</div>

    <script src="/js/editDelete.js"></script>